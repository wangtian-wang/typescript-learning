```js
    类型定义方式： type = ......
    接口定义方式： interface name {}
        使用了接口 | 类型  作为类型注解，就一定要将接口里面定义的必选的参数实现
```
## 交叉类型：
```js
 type Wtype = number & boolean


 使用方法：

    interface Person {}
    interface Fruit {}
    type MyType = person | fruit
    type myType = person & fruituiitu




```